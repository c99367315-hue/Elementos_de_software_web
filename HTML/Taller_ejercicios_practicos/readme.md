● Por qué los enlaces son verdes y no rojos 
R//: Porque se puso el color rojo a la etiqueta a, y y no a las palabras que contenian el link, entonces abajo con las clases se modifican las palabras con las que se contiene el link y se les puso un color verde

● Hacer que los enlaces sean rojos 
R//: Lo que se tiene que hacer es poner una etiqueta a, y se pone color: red !important, y ya se pone rojo

● Rehacer el HTML usando <div> en lugar de <ul> y <li>. ¿Qué pasa? 
R//: No pasa nada, sino que en vez de elemento de una lista se pone como un contenedor, pero se sigue aplicando las cosas que pusimos en css porque en css se utilizan son clases

● Comentar el CSS que no se puede tocar y reescribir este CSS usando una clase por selector para que se vea igual.
R//: Lo que se tiene que hacer es a cada elemento asignarle una nueva clase y despues modificar cada una en css con los mismos parametros si es un titulo o un link y ya 